services:
  - type: web
    name: niftyedge
    runtime: static
    buildCommand: echo "static site"
    staticPublishPath: .
    pullRequestPreviewsEnabled: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
